# PRUEBA TEMPORAL — se elimina apenas tengamos el resultado.
# Llama a /api/norden en el preview y verifica la respuesta real: la función
# nunca corrió de verdad, solo se probó el parser contra un fragmento.
name: PRUEBA — /api/norden en vivo

on: pull_request

permissions:
  contents: read

jobs:
  prueba:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node prueba-norden-vivo.mjs
